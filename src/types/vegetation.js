// @ts-check
export {}

/**
 * @typedef {Object} VegetationGeometries
 * @property {import('three').BufferGeometry[]} surrounding - cast shadows onto the simulation
 * @property {import('three').BufferGeometry[]} background  - visual context only
 */
